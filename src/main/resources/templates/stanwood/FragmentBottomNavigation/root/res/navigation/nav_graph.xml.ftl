<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/${navGraphName}">

	<!-- TODO : Add Bottom Navigation destinations and set start destination. 
    Note: Actions needs to pop to backstack when navigating between destinations.
    This can be done in code via NavOptions or just here when using SafeArgs e.g.:
    <action android:id="@+id/action_name" app:destination="@id/destination" app:popUpTo="@id/${navGraphName}" app:popUpToInclusive="true"/>
    -->
</navigation>